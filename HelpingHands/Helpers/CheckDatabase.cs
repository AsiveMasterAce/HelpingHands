using HelpingHands.Data;

namespace HelpingHands.Helpers
{
    public class CheckDatabase
    {
        //private readonly ApplicationDbContext _context;
        //public bool SuburbExists(string suburbName, int postalCode)
        //{
        //    return this.Suburbs.Any(s => s.Name.ToLower() == suburbName.ToLower() && s.PostalCode == postalCode);
        //}
    }
}
